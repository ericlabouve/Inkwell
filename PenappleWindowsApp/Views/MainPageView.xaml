<Page
    x:Class="PenappleWindowsApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PenappleWindowsApp"
    xmlns:ViewModels="using:PenappleWindowsApp.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".075*"/>
            <ColumnDefinition Width=".325*"/>
            <ColumnDefinition Width=".6*"/>
        </Grid.ColumnDefinitions>

        <Frame Name="sideBar" Grid.Column="0"/>
        <Border Grid.Column="1" BorderBrush="#e4d0f5" BorderThickness="2 0 2 0">
            <Frame Name="leftFrame"/>
        </Border>
        <Frame Name="rightFrame" Grid.Column="2"/>
        <Frame Name="profileFrame" Grid.Column="1" Grid.ColumnSpan="2"/>
    </Grid>
</Page>
